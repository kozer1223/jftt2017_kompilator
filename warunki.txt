WARUNKI:

1) a <= b

tmp := a - b
if tmp == 0 goto true_block
goto end
label true_block
[ true_block ]
label end

tmp := a - b
if tmp == 0 goto end
label false_block
[ false_block ]
label end

tmp := a - b
if tmp == 0 goto true_block
label false_block
[ false_block ]
goto end
label true_block
[ true_block ]
label end

<assembly>
             [LOAD a]
             SUB b
             JZERO true_block
false_block: (false_block)
             JMP end
true_block:  (true_block)
end:
</assembly>

2) a >= b

równoważne z b <= a dla a,b - symbole
dla b = const
a >= b równoważne a > b-1, dla b = 0 zawsze true

3) a < b

równoważne z b > a dla a,b - symbole
dla b = const
a < b równoważne z a <= b-1, dla b = 0 zawsze false

4) a > b
(negacja a <= b)

tmp := a - b
if tmp == 0 goto end
label true_block
[ true_block ]
label end

tmp := a - b
if tmp == 0 goto false_block
label true_block
[ true_block ]
goto end
label false_block
[ false_block ]
label end

<assembly>
             [LOAD a]
             SUB b
             JZERO false_block
true_block:  (true_block)
             JMP end
false_block: (false_block)
end:
</assembly>

5) a = b

tmp := a - b
if tmp == 0 goto eq_check
goto end
label eq_check
tmp := b - a
if tmp == 0 goto true_block
goto end
label true_block
[ true_block ]
label end

tmp := a - b
if tmp == 0 goto eq_check
label false_block
[ false_block ]
goto end
label eq_check
tmp := b - a
if tmp == 0 goto end
goto false_block
label end

tmp := a - b
if tmp == 0 goto eq_check
label false_block
[ false_block ]
goto end
label eq_check
tmp := b - a
if tmp == 0 goto true_block
goto false_block
label true_block
[ true_block ]
label end

<assembly>
             [LOAD a]
             SUB b
             JZERO eq_check
false_block: (false_block)
             JMP end
eq_check:    LOAD b
             SUB a
             JZERO true_block
             JMP false_block
true_block:  (true_block)
end:
</assembly>

6) a <> b
(negacja a = b)

tmp := a - b
if tmp == 0 goto eq_check
label true_block
[ true_block ]
goto end
label eq_check
tmp := b - a
if tmp == 0 goto end
goto true_block
label end

tmp := a - b
if tmp == 0 goto eq_check
label true_block
[ true_block ]
goto end
label eq_check
tmp := b - a
if tmp == 0 goto false_block
goto true_block
label false_block
[ false_block ]
label end

<assembly>
             [LOAD a]
             SUB b
             JZERO eq_check
true_block:  (true_block)
             JMP end
             LOAD b
             SUB a
             JZERO false_block
             JMP true_block
false_block: (false_block)
end:
</assembly>

7) alternate a = b (for small constants)

tmp := a - b
if tmp == 0 goto eq_check
label false_block
[ false_block ]
goto end
label eq_check
tmp := a - (b-1)
if tmp == 0 goto false_block
[ true_block ]
label end
